# User Guide

## Getting Started

1. Install dependencies: `npm install`
2. Build the project: `npm run build`
3. Run tests: `npm test`

## Error Handling

The project includes custom error types:
- ValidationError
- NotFoundError

Use the `handleError` utility to process errors consistently.
