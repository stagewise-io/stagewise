Use `useCurrentFrame()` for text animation. For typewriter, reveal string char by char.

Typewriter Example in `assets/text-animations-typewriter.tsx` with blinking cursor + pause after first sentence. Always use string slicing for typewriter. Never use per-character opacity.

See `assets/text-animations-word-highlight.tsx` for word highlight animation like highlighter pen.
